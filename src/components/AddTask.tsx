import { FC } from 'react';
import { Button } from './Button';

export const AddTask: FC = () => {
    return (
        <div>
            <input />
            <Button title='+' />
        </div>
    );
};